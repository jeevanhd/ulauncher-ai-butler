[project]
name = "ai-butler-daemon"
version = "0.1.0"
description = "Local daemon running Needle for AI Butler ulauncher extension"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.32",
    "needle @ git+https://github.com/cactus-compute/needle.git",
]

[tool.uv.sources]
# needle isn't on PyPI - installed from source, same as the manual venv setup


[tool.uv]
package = false
